String slicing is creating a substring from a portion if another string.

String.slice(start, end)

example 1
#file Start.js
const fullName = "Bro Code";

let firstName = fullName.slice(0, 3);
let lastName = fullName.slice(4, 8);

let lastChar = fullName.slice(-1); //very last index

console.log(firstName);
console.log(lastName);

example 2 
#file Start.js
const email = "Bro1@gmail.com";

let username = email.slice(0, email.indexOf("@"));  //ตั้งแต่ index 0 to the first “@”  so we get Bro1

let extension = email.slice(email.indexOf("@")); //@gmail.com
let extension = email.slice(email.indexOf("@") + 1); //gmail.com

console.log(username);
console.log(extension);
